# requirements.txt

# Final, stable configuration for Streamlit Cloud deployment
streamlit
pandas
# Pinning a specific version of pysqlite3-binary to fix the chromadb RuntimeError
pysqlite3-binary==0.4.6
langchain==0.1.14
langchain-community==0.0.30
# Pinning chromadb and protobuf to versions compatible with each other
chromadb==0.4.13
protobuf==3.20.3
langchain-chroma
transformers
torch
scikit-learn
sentence-transformers